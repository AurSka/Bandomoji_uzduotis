@model IEnumerable<Bandomoji_uzduotis.Models.Filmas>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>List</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IsleidimoData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FilmoZanras)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsleidimoData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FilmoZanras)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Pavadinimas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Pavadinimas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Pavadinimas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
